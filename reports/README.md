Future R-based report outputs can live here.

Suggested next step:
- add an R script that reads `../outputs/cross_patch_results.csv`
- produce comparison plots for `evaluation_score_b`, `weighted_mae`, and `weighted_mse`
- keep modeling in Python and keep presentation figures here
